# CN31 Token Server

## Quick Start

```bash
# Install dependencies
pip install -r requirements.txt

# Start server
python server.py --port 5050

# With custom TTL
python server.py --port 5050 --ttl 300